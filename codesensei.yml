redis:
    host: 127.0.0.1
    port: 
    password:
    resque_namespace:

database:
    postgres_host:
    postgres_port:
    postgres_db: codesensei
    postgres_user: 
    postgres_password:
    
system:
    memory_limit:
    
    # Maximum memory limit per program
    # Default value: 128000 kilobytes
    max_memory_limit: 

    cpu_time_limit: 
    
    # Maximum cpu time limit
    # Default value: 10 seconds
    max_cpu_time_limit: 10

    cpu_extra_time_limit:
    max_cpu_extra_time_limit: 5

    wall_time_limit:
    max_wall_time_limit: 20

    stack_limit:
    max_stack_limit: 128000

    iterations:
    max_iterations: 5

    max_processes_and_or_thread:
    max_max_processes_and_or_thread:

    redirect_stderr_to_stdout: false

    allow_network: 

    enable_callback: true
    enable_compiler_options:
    enable_command_line_arguments:
    callback_timeout:
    callback_max_retries:
    submission_cache_duration:
    
worker:
    interval:
    count:
    max_queue_size:

server:
    allow_origin:
    allow_ip:
    disallow_origin:
    disallow_ip: